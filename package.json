{
  "name": "mhhorizoncox",
  "version": "1.0.0",
  "description": "Static gateway website for the MH Horizon ecosystem.",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Static site ready: no build step required.')\"",
    "check": "node --check scripts/cinematic.js && node --check scripts/gateway-to-hub.js && node --check scripts/gateway-three.js && node --check scripts/gateway.js && node --check scripts/hubs.js && node --check scripts/main.js",
    "test": "npm run check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
